# ---- Web framework ----
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ---- Settings & validation ----
pydantic==2.12.5
pydantic-settings==2.5.2
email-validator==2.2.0

# ---- Database / ORM / migrations ----
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2

# ---- Auth / security ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# ---- Environment ----
python-dotenv==1.0.1

# ---- PDF export (Phase 3) ----
Jinja2==3.1.4
weasyprint==69.0

# ---- AI (Phase 4) ----
# Google's current GA SDK for the Gemini API. The older
# "google-generativeai" package is legacy/unmaintained per Google's
# own docs -- this is the recommended replacement.
google-genai==2.23.0
